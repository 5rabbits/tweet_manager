require 'spec_helper'

describe TweetManager do
  it 'has a version number' do
    expect(TweetManager::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
